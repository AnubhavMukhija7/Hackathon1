import express from 'express';

const router = express.Router();

// send html with dropdown
router.get('/', (req, res) => {});

// show result of corresponding year...
router.get('/:year', (req, res) => {});

export default router;
